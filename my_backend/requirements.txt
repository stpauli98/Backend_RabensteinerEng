# Essential packages for server to start
Flask==3.1.3
Jinja2>=3.1,<4  # Auth-email template rendering (transitive via Flask, listed explicitly)
Flask-Cors==6.0.0
Flask-Limiter==3.5.0
Werkzeug==3.1.6
flask-socketio>=5.4.0,<6.0
gunicorn==22.0.0
python-dotenv==1.2.2  # security: CVE-2026-28684
requests>=2.31.0,<3.0
pytz==2024.1
APScheduler==3.10.4
supabase>=2.7.0,<3.0  # 2.7+ fixes proxy parameter issues (gotrue/httpx pins no longer needed)
PyJWT>=2.8.0  # Local HS256 verification of Supabase-issued JWTs
marshmallow>=3.20.0  # JSON schema validation for upload endpoints
anthropic>=0.39,<1.0  # Claude SDK for the in-app assistant chatbot


# Basic data handling (lightweight)
pandas>=2.1.4,<3.0
numpy>=1.26.3,<3.0

# Required by cloud.py blueprint
scikit-learn>=1.5.1,<2.0
matplotlib>=3.8.2,<4.0

# Required by anomaly detection pipeline (STL decomposition)
statsmodels>=0.14.1,<1.0

# Heavy packages
seaborn>=0.13.1,<1.0
tensorflow==2.19.0
keras==3.12.1  # Pin to TF 2.20/Keras 3.12 line for model file compatibility; 3.12.1 patches CVE-2026-1669/1462 + PYSEC-2026-73 (stays within 3.12.x)
lightgbm>=4.3.0,<5.0

# Testing packages
pytest==8.3.4
pytest-cov==6.0.0
pytest-flask==1.3.0
pytest-mock==3.14.0

# Stripe
stripe==7.4.0

# Build tooling (security-pinned)
wheel>=0.46.2  # security: CVE-2026-24049 (build-time only)